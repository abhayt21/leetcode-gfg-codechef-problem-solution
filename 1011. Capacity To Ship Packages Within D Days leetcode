class Solution {

    public int shipWithinDays(int[] weights, int days) {
      int  min = 0 ;
      int max = 0 ;

      for(int w: weights){
          min = Math.max(w.win) ;
          max += w ;
      }  

      int retVal = 0 ;
      while(min <=max){
          int mid =(min + max ) ;
          int daysRequired = numb }
}
